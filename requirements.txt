# Pinned to the known-good set verified locally (select Python 3.11 on Streamlit Cloud).
# Only the packages the app actually imports — keeps the cloud build fast and reproducible.
streamlit==1.50.0
pydantic==2.12.5
pydantic-ai==0.8.1
anthropic==0.105.2
langgraph==0.6.11
langchain-core==0.3.83
neo4j==5.28.3
python-dotenv==1.2.1
